$(document).ready(() => {
  $("#searchForm").on("submit", e => {
    // let searchText = $("#searchbar").val();
    // getMovies(searchText);
    // e.preventDefault();
    alert("form submitted");
  });
});
